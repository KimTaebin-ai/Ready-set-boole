#ifndef GRAY_CODE_HPP
# define GRAY_CODE_HPP

# include <cstdint>

// Time complexity:  O(1)
// Space complexity: O(1)
// Allowed: bitwise ops (& | ^ << >>) and comparison ops only.
// Forbidden: arithmetic ops (+ - * / %).
uint32_t gray_code(uint32_t n);

#endif
