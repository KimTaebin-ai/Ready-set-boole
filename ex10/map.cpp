#include "map.hpp"

double map(uint16_t x, uint16_t y)
{
	(void)x;
	(void)y;
	// TODO: interleave the bits of x and y into a 32-bit integer, then
	// normalise to [0, 1] by dividing by 2^32.
	return 0.0;
}
