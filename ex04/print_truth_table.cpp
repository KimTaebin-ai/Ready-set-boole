#include "print_truth_table.hpp"

void print_truth_table(const std::string& formula)
{
	(void)formula;
	// TODO: enumerate all 2^k assignments of the variables in `formula`,
	// substitute them into the formula, evaluate via the RPN evaluator,
	// and print one row per assignment.
}
